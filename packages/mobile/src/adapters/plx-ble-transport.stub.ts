/**
 * BLE mesh: `plx-ble-mesh-transport.ts` + nativo `MesherBleNative` (Android Kotlin / iOS Swift).
 * Plantillas y plugin: `plugins/mesher-ble-native/`, `plugins/withMesherBle.js`.
 * Sin módulo nativo → `MockBleMeshTransport` vía `mesh/createMeshTransport.ts`.
 */
export const BLE_PLX_INTEGRATION_PENDING = false;
